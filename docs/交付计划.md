# 交付计划

> 最后更新: 2026-06-17 ｜ P0、P1、P2 修复已完成 ✅ | P3-1 三项全部完成 ✅

---

## Phase 1: 基础架构 (已完成 ✅ — 含待修复问题)

| 模块 | 状态 | 说明 |
|------|------|------|
| Monorepo 项目结构 | ✅ | backend / web / pda / gateway / docs |
| FastAPI 后端框架 | ✅ | main.py 注册 11 组路由，含 lifespan + init_db/seed_db |
| PostgreSQL 数据库设计 | ✅ | 18 张 SQLAlchemy Model，含迁移骨架 |
| PC Web 前端 (React 18 + Ant Design 5) | ✅ | 12 页面全部实现，38 个 API 函数 |
| PDA 原生端 (Kotlin + Hilt + Room) | ✅ | 5 ViewModel + 4 Repository + 6 页面 |
| PDA React Native (Expo) | ✅ | 5 页面全部实现 |
| Modbus HAL 实现 | ✅ | `hal/modbus.py` 完整 `Amkn8702g` 类，含 SlotPoller |
| 智能条码识别引擎 | ✅ | `utils/barcode.py` 含模糊匹配框架 |
| OCR 文字识别 | ✅ | `utils/ocr.py` PaddleOCR 懒加载 |

### ⚠️ Phase 1 待修复问题

| # | 问题 | 影响 | 建议操作 |
|---|------|------|----------|
| ~~P1-1~~ | ~~`hal/modbus_tcp.py` — 与 `modbus.py` 重复的 `Amkn8702g` 实现，且存在 bug~~ | ~~代码维护、潜在混淆~~ | ✅ 已删除 |
| ~~P1-2~~ | ~~`utils/barcode.py` DB 查询完全失效~~ | ~~条码扫码永无法匹配物料主数据~~ | ✅ 已修复 |
| ~~P1-3~~ | ~~PDA React Native 使用 `localStorage`（Web API），RN 中不可用~~ | ~~导致 RN 构建在真机上崩溃~~ | ✅ 已修复: 改用 `@react-native-async-storage/async-storage` |
| P1-4 | PDA Kotlin 缺少单元测试 — `TEST_PLAN.md` 已定义 50+ 用例但未实现 | 代码质量保障 | 按 TEST_PLAN.md 实现测试 |
| ~~P1-5~~ | ~~Web 仪表盘 `pendingReceipts` 和 `pendingIssues` 始终显示 0~~ | ~~仪表盘数据不准确~~ | ✅ 已修复: 从 API 获取真实计数 |


## Phase 2: 核心业务逻辑 (已完成 ✅)

### 2.1 FIFO 尾数优先算法

| 项 | 状态 | 详情 |
|—|------|------|
| 四种策略 (tail_first, time_fifo, mixed) | ✅ | 代码已完成 |
| 贪心选择算法 | ✅ | 支持 BOM 多行分配 |
| 替代料查询 | ✅ | `check_alternative_material()` 已实现 |
| 单元测试 | ✅ | 16 个用例覆盖所有策略、短缺、空库存等边界 |
| ⚠️ `last_in_time` vs `first_in_time` | ⚠️ | 待客户确认 FIFO 策略 |

### 2.2 LED 亮灯控制

| 项 | 状态 | 详情 |
|—|------|------|
| Modbus 协议帧 & Protocol 2 线圈操作 | ✅ | `hal/modbus.py` 中 `set_led()` 支持 4 线圈控制 |
| `LedService` 类结构 | ✅ | 含双 Worker: 内存队列 + DB 轮询 |
| ✅ `LedService` 已接入应用生命周期 | ✅ | `main.py` lifespan 中实例化并启动 |
| ✅ `light_slot()` 修复 slot_id 解析 | ✅ | 从 ShelfSlot 表查询 face/board_addr/slot_num |
| ✅ `clear_all_leds()` 优化 | ✅ | 改为按面板批量发送 OFF 指令，显著减少请求次数 |
| ✅ 后台 Worker 消费 `LedCommand` 表 | ✅ | `_db_command_worker()` 每 2s 轮询 `queued` 记录，自动发送硬件并更新状态 |
| ✅ `/api/issues/{id}/assign` 写 DB + 触发亮灯 | ✅ | 写入 LedCommand (status=queued)，由后台 Worker 自动处理 |

### 2.3 XR 点料机对接

| 项 | 状态 | 详情 |
|—|------|------|
| 端到端流程（上传 → 自动配对 → 确认上架） | ✅ | 含时间窗口 (±5s)、状态管理 |
| `handle_xr_upload()` | ✅ | 创建 Batch → 自动配对 → 返回匹配结果 |
| `confirm_restock()` | ✅ | 更新库存状态、创建 Transaction |
| API 端点 (GET/POST upload/POST match/POST confirm) | ✅ | 全部注册 |
| ⚠️ `_parse_reel_id()` 为 Stub | ⚠️ | 需根据实际条码格式定制 |
| ⚠️ `operator` 字段未写入 | ⚠️ | XrBatch.operator 未在 upload 时保存 |

### 2.4 入库流程 (Receipt)

| 项 | 状态 | 详情 |
|—|------|------|
| 创建入库单 `POST /receipts` | ✅ | 自动编号 `RC-YYYYMMDD-NNN` |
| 扫码入库 `POST /receipts/{id}/scan` | ✅ | 创建 InventoryPallet + ReceiptPallet |
| 自动分配空储位 | ✅ | 扫码时查找空位 |
| ✅ 扫码数量可配置 | ✅ | 从 `ReceiptScanRequest.qty` 读取，未传时默认 1.0 |
| ✅ 状态机已完整（draft → confirmed → completed） | ✅ | `PUT /receipts/{id}/confirm` + `PUT /receipts/{id}/complete` |
| 入库单列表/详情 | ✅ | `GET /receipts` + `GET /receipts/{id}` |
| ✅ 手动分配储位接口 | ✅ | 新增 `PUT /receipts/{id}/assign-slot`，含占位校验 |
| ✅ `customer_id` 写入修复 | ✅ | `ReceiptCreate` 新增 `customer_id: int = 1`，创建时正确写入 |
| ✅ 重复条码校验防护 | ✅ | 增加 `if parsed.extra else ""` 空值保护 |

### 2.5 出库流程 (Issue)

| 项 | 状态 | 详情 |
|—|------|------|
| 发料单详情 `GET /issues/{id}` | ✅ | 含明细行 |
| FIFO 计算 `POST /issues/{id}/calculate` | ✅ | 调用 FIFO Service，含物料编码/名称 |
| LED 分配 `POST /issues/{id}/assign` | ✅ | 创建 LedCommand 记录 |
| 确认拣料 `POST /issues/{id}/confirm-pick` | ✅ | 更新库存 + Transaction |
| 创建发料单 `POST /issues` | ❌ | 只能通过 BOM generate 创建 (P3 计划) |
| ✅ 拣料数量修复 | ✅ | 从 InventoryPallet.quantity 读取实际数量替代硬编码 |
| ✅ `cleared_leds` 已修复 | ✅ | 确认拣料后清除 LedCommand 并返回已清除 slot_id 列表 |
| ✅ 材料编码/名称已修复 | ✅ | calculate 响应中从 MaterialMaster 查询真实 code/name |

### 2.6 防呆校验

| 项 | 状态 | 详情 |
|—|------|------|
| 入库重复条码校验 | ✅ | 基础实现 |
| 出库重复拣料校验 | ✅ | 已实现 |
| ✅ 条码模糊匹配 DB 查询 | ✅ | DB 查询已修复: 精确匹配 → 模糊评分 (>0.5 准入) |
| ✅ 数量校验（拣料不可超过可用量） | ✅ | confirm-pick 已增加 min(available, remaining) 保护 |
| ✅ 部分消耗支持 | ✅ | 拣料少于整盘时仅减少库存不耗尽 |
| ✅ 储位容量校验 | ✅ | ShelfSlot 新增 max_quantity 字段，入库/分配时校验 |
| ✅ Transaction 记录 | ✅ | 出库确认自动写入 Transaction |
| ⚠️ 统一校验层/中间件 | ⏳ | P3 计划 |

### 2.7 库存管理

| 项 | 状态 | 详情 |
|—|------|------|
| 库存列表 `GET /inventory` | ✅ | 带物料编码和料架编号的 JOIN 查询 |
| 跟踪中库存 `GET /inventory/tracking` | ✅ | 等待上架的库存 |
| ✅ 库存变更 `PUT /inventory/{id}` | ✅ | 支持 quantity/status/shelf_slot_id 部分更新，含储位占用校验 |
| 单盘详情 | ⏳ | P3 计划 |

### 2.8 BOM 管理

| 项 | 状态 | 详情 |
|—|------|------|
| BOM 上传解析 `POST /bom/upload` | ✅ | openpyxl 解析 Excel |
| BOM 详情 `GET /bom/{id}` | ✅ | 含材料名称解析 |
| 生成发料单 `POST /bom/{id}/generate-issue` | ✅ | 创建 IssueOrder + IssueDetail |
| ✅ BOM 列表 `GET /bom` | ✅ | 含物料数量统计、支持 customer_id 筛选 |
| ✅ BOM 删除 `DELETE /bom/{id}` | ✅ | 含关联发料单校验，防止误删 |
| ✅ BOM 更新 `PUT /bom/{id}` | ✅ | 支持修改 bom_name / product_code / customer_id |
| ✅ 发料单号修复 | ✅ | 改为按日顺序编号 `IS-YYYYMMDD-NNN`（与入库单一致） |

### 2.9 报表

| 项 | 状态 | 详情 |
|—|------|------|
| 日报 `GET /reports/daily` | ✅ | 完整重写 |
| ✅ 期初库存 | ✅ | 从 transaction 历史计算报告日期零点库存快照 |
| ✅ 材料名称 | ✅ | 从 MaterialMaster 查询真实名称 |
| ✅ 余额计算 | ✅ | `closing = opening + in - out`，含 `total_balance` |
| ✅ 在架/跟踪数量 | ✅ | 从 InventoryPallet 实时统计 |
| ✅ 客户汇总 `customer-summary` | ✅ | 按物料分类聚合，含材料明细 |
| ✅ 异常处理 | ✅ | 移除通用 `except:`，改为 HTTPException 500 + traceback |

### 2.10 料架/Slot 管理

| 项 | 状态 | 详情 |
|—|------|------|
| 料架 CRUD (GET/POST/PUT/DELETE) | ✅ | 完整实现 |
| Slot 列表 `GET /shelves/{id}/slots` | ✅ | 按面板分组排序 |
| 创建 Slot `POST /shelves/{id}/slots` | ❌ | Schema 有定义但无路由 (P3 计划) |
| Slot 更新/删除 | ❌ | 无端维护 (P3 计划) |

### 2.11 认证/用户/Settings

| 项 | 状态 | 详情 |
|—|------|------|
| 登录 `POST /auth/login` | ✅ | JWT 签发 |
| 当前用户 `GET /auth/me` | ✅ | Token 验证 |
| 用户 CRUD | ✅ | 含角色 (admin/supervisor/operator/readonly) |
| 系统设置 CRUD | ✅ | key-value JSON |

---

## Phase 3: 前端完善 (已完成 80% ✅)

### PC Web (全部 12 页面 — 100% 完成)

| 页面 | 状态 | 详情 |
|------|------|------|
| 登录页 | ✅ | 表单验证、Token 持久化 |
| 仪表盘 | ✅ | 统计卡片 + 数据表，含连接超时提示 |
| 物料管理 | ✅ | CRUD + 搜索分页 |
| 料架管理 | ✅ | CRUD + Slot 网格抽屉 |
| 库存查询 | ✅ | 列表/筛选/状态标签 |
| 入库管理 | ✅ | 分步流程：创建 → 扫码 → 确认 |
| 发料管理 | ✅ | 列表 + 计算/分配/确认 |
| XR 点料机 | ✅ | 上传表单 + 记录列表 |
| BOM 管理 | ✅ | Excel 上传 + 解析结果 |
| 报表统计 | ✅ | 日期范围查询 + 明细表 |
| 系统设置 | ✅ | key-value 编辑 |
| 用户管理 | ✅ | CRUD + 角色标签 |

### PDA Kotlin (Compose) — 已完成 ~75%

| 页面 | 状态 | 详情 |
|------|------|------|
| 首页 | ⚠️ | 仅静态文字，无数据展示 |
| 入库 | ✅ | 扫码循环 + 结果卡片 |
| 出库 | ✅ | 选择单号 → 扫码确认 |
| 退库/补料 | ✅ | 独立 RestockScreen |
| 跟踪查询 | ✅ | 选项卡切换 + 物料卡片 |
| 设置 | ⚠️ | 只读显示，无法编辑服务器地址 |

### PDA React Native (Expo) — 代码完成但含崩溃 bug ⚠️

| 页面 | 状态 | 详情 |
|------|------|------|
| 全部 5 页面 | ✅ | UI + API 集成全部实现 |
| ⚠️ `localStorage` 在 RN 中不可用 | ❌ | `api/index.ts` 和 `authStore.ts` 使用 Web API |
| ⚠️ `Base_URL` 硬编码 `localhost:8080` | ⚠️ | 需改为可配置 |

---

## Phase 4: 部署上线 (计划中 📋)

| 项 | 状态 | 详情/前置条件 |
|---|------|--------------|
| Docker Compose 编排 | ✅ | 5 服务 (db/backend/web/gateway/pgadmin) |
| Nginx 反向代理 | ✅ | gateway 配置完成 |
| Web 容器化 (Dockerfile + nginx.conf) | ✅ | Vite IIFE 单文件构建 |
| 后端容器化 (Dockerfile) | ✅ | Python 3.11 |
| ⚠️ 硬件联调测试 | ❌ | 需现场连接 AMKN8702G + AMKN7141 验证 |
| ⚠️ 性能优化 | ❌ | 待评估 (并发请求、Modbus 轮询频率) |
| ⚠️ 生产环境部署 | ❌ | 含 TLS、备份策略 |
| ⚠️ 用户培训 | ❌ | 操作手册 + 现场培训 |
| ⚠️ 数据库迁移 (Alembic) | ⚠️ | 基础结构存在但未确认 |

---

## 优先级修复清单 (按影响排序)

| 优先级 | 任务 | 对应问题 | 预估工时 |
|--------|------|----------|----------|
| ~~P0~~ | ~~修复 `barcode.py` DB 查询 — 物料扫码无法匹配~~ | ~~P1-2, P2-6~~ | ✅ 已完成 |
| ~~P0~~ | ~~入库写入 customer_id + 修复重复扫码崩溃 + 硬编码数量~~ | ~~P2-4~~ | ✅ 已完成 |
| ~~**P1**~~ | ~~LedService 接入生命周期 + 修复 light_slot 参数~~ | ~~P2-2~~ | ✅ 已完成 |
| ~~**P1**~~ | ~~入库状态机推进 (draft → confirmed → completed)~~ | ~~P2-4~~ | ✅ 已完成 |
| ~~**P1**~~ | ~~修复出库 cleared_leds + 硬编码数量 + material_code~~ | ~~P2-5~~ | ✅ 已完成 |
| ~~**P1**~~ | ~~删除 `modbus_tcp.py` 重复文件~~ | ~~P1-1~~ | ✅ 已完成 |
| ~~**P1**~~ | ~~修复 PDA RN localStorage bug~~ | ~~P1-3~~ | ✅ 已完成 |
| ~~**P1**~~ | ~~仪表盘 pendingReceipts/pendingIssues 始终为 0~~ | ~~P1-5~~ | ✅ 已完成 |
| ~~**P2**~~ | ~~实现手动分配储位 `PUT /receipts/{id}/assign-slot`~~ | ~~P2-4~~ | ✅ 已完成 |
| ~~**P2**~~ | ~~BOM 列表/删除/更新端点~~ | ~~P2-8~~ | ✅ 已完成 |
| ~~**P2**~~ | ~~实现后台 LedCommand Worker~~ | ~~P2-2~~ | ✅ 已完成 |
| ~~**P2**~~ | ~~修复日报表 (期初库存、余额计算、材料名称)~~ | ~~P2-9~~ | ✅ 已完成 |
| ~~**P3-1**~~ | ~~FIFO 单元测试~~ | ~~P2-1~~ | ✅ 已完成 16/16 用例 |
| **P3-2** | PDA Kotlin 单元测试 | P1-4 | 12h |
| ~~**P3-1**~~ | ~~数量校验 + 储位容量校验~~ | ~~P2-6~~ | ✅ 已完成 |
| ~~**P3-1**~~ | ~~库存变更 API (PUT /inventory)~~ | ~~P2-7~~ | ✅ 已完成 |
| **P3-2** | PDA Kotlin 首页数据化 + 设置可编辑 | Phase 3 | 4h |
| **P4** | 硬件联调测试 | Phase 4 | 16h |
| **P4** | 生产环境部署脚本 | Phase 4 | 8h |

---

## 总体进度

| Phase | 总项数 | 已完成 | 待修复/进行中 | 进度 |
|-------|--------|--------|--------------|------|
| Phase 1: 基础架构 | 7 大项 | 7 | 1 个待修复问题 (P1-4) | 97% |
| Phase 2: 核心业务逻辑 | 27 子项 | 26 | 1 项待确认 (FIFO 策略) | 96% |
| Phase 3: 前端完善 | 13 页面 | 12 页面 | 1 页面待完善 | 92% |
| Phase 4: 部署上线 | 5 项 | 3 项 | 2 项待完成 | 60% |
| **总计** | **52 项** | **48 项** | **4 项** | **~92%** |
