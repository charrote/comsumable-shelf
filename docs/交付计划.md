## 开发计划

### Phase 1: 基础架构 (已完成 ✅)
- [x] Monorepo 项目结构搭建
- [x] FastAPI 后端框架
- [x] PostgreSQL 数据库设计
- [x] 硬件抽象层 (Modbus TCP + RTU)
  - ⚠️ 存在 `hal/modbus.py` 和 `hal/modbus_tcp.py` 两套重复的 `Amkn8702g` 实现，需二选一清理
- [x] 智能条码识别引擎
  - ⚠️ `parse_barcode()` 中物料 DB 查询语法错误，无法真正匹配物料
- [x] PC Web 前端 (React 18 + Ant Design 5)
- [x] PDA 端 (Kotlin + Jetpack Compose)

### Phase 2: 核心业务逻辑 (进行中 🚧)
- [ ] **FIFO 尾数优先算法完善** — 代码已完成（4 种策略），缺单元测试
- [ ] **LED 亮灯控制协议实现**
  - [x] Modbus 协议帧及 LED Protocol 2 位操作实现完成
  - [ ] `LedService` 未接入应用生命周期（未在 `main.py` 中启动）
  - [ ] `/api/issues/{id}/assign` 仅写 DB 记录，未实际调用硬件发灯
  - [ ] 无后台 Worker 消费 `LedCommand` 记录
- [ ] **XR 点料机数据对接** — 端到端流程完成，`_parse_reel_id()` 为 stub，需根据实际条码格式完善
- [ ] **出入库业务流程联调**
  - [x] 入库 `POST /api/receipts` 创建单据完成
  - [ ] 入库扫码数量硬编码为 50.0，需改为实际扫描数量
  - [ ] 入库状态机不完整（卡在 `draft`，未推进到 `confirmed`/`completed`）
  - [ ] 缺少手动分配储位 `PUT /api/receipts/{id}/assign-slot` 接口
  - [x] 出库 FIFO 计算接口 `/api/issues/{id}/calculate` 完成
  - [ ] 出库确认拣料 `picked_qty` 硬编码为 1.0
  - [ ] 出库确认未实际清除 LED（`cleared_leds` 始终返回空数组）
- [ ] **防呆校验机制**
  - [x] 重复条码校验 (入库)、重复拣料校验 (出库)
  - [ ] **条码模糊匹配的 DB 查询失效**，需修复 `parse_barcode()` 中的物料查询
  - [ ] 缺少数量校验（拣料不可超过可用量）
  - [ ] 缺少储位容量校验
  - [ ] 缺少统一校验层/中间件

### Phase 3: 前端完善 (计划中 📋)
- [ ] PC Web 全部页面开发
- [ ] PDA 扫码入库/出库功能
- [ ] 报表统计页面
- [ ] 用户管理权限

### Phase 4: 部署上线 (计划中 📋)
- [ ] 硬件联调测试
- [ ] 性能优化
- [ ] 生产环境部署
- [ ] 用户培训
